# data/ —— 不要從 zip 覆蓋這個資料夾

這裡是你**真正的資料**（每天收集的論文）。

## ⚠️ 上傳新版時，不要動 data/

`index.json` 曾經被一個空的版本覆蓋掉，導致 9 天、143 篇論文
在網站上「消失」（日檔明明還在，但索引說只有一天）。

現在 `index.json` 和 `search.json` 都是**衍生資料** ——
每次跑 workflow 都會從實際的日檔完全重建，所以就算被覆蓋也會自我修復。

但還是不要覆蓋。

## 檔案

| | |
|---|---|
| `YYYY-MM-DD.json` | 每日文獻（**真正的資料**）|
| `index.json` | 日期索引 —— 衍生，自動重建 |
| `search.json` | 搜尋索引 —— 衍生，自動重建 |
| `_cache_v3.json` | PMID 快取 —— 避免重複付費 |
