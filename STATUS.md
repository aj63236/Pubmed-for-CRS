# 腸嚐新知 — 專案現況

> 每日自動抓 PubMed 大腸直腸外科新論文，用 Claude 做批判性分析，發佈到一個手機網站。
> 給接手的人：讀完這份就知道現在在哪、下一步該做什麼。

**最後更新：** 2026-07-18

---

## 一句話

**PubMed 每天有 60–80 篇相關論文。這個系統幫你選出該讀的 10–17 篇，用中文告訴你「值不值得讀」，並且會比對你之前讀過的文獻告訴你「這篇跟你已知的一致還是矛盾」。**

---

## 現在能用的東西

| | 網址 |
|---|---|
| **網站** | https://aj63236.github.io/Pubmed-for-CRS/ |
| **原始碼** | https://github.com/aj63236/Pubmed-for-CRS |
| **手機 App** | Safari 開網站 → 分享 → 加入主畫面（PWA，有圖示、全螢幕、可離線） |

---

## 運作方式

```
每天台灣時間 06:00（GitHub Actions 自動跑，電腦不用開機）
    │
    ├─ 1. 掃描 PubMed 當天全部論文（60–80 篇）        ← 免費
    │
    ├─ 2. 本機排序：期刊 IF + 研究設計               ← 免費
    │      分數 = min(IF, 25) × 0.5 + 研究設計權重 × 2
    │
    ├─ 3. 選片                                       ← 這步之後才花錢
    │      🔖 追蹤命中（無視分數，上限 6 篇）
    │      + 高分前 10 篇
    │      + 當天所有 Review（上限 8 篇）
    │
    ├─ 4. 用 TF-IDF 從過去的論文找出相關前作          ← 免費
    │
    ├─ 5. 送 Claude：中文摘要 + 批判性分析 + 與前作比較
    │
    └─ 6. 存成 JSON → commit → push → 網站自動更新
```

---

## 每篇論文產出什麼

**三個判讀標籤**（掃一眼就決定去留）
- `評分 7/10` — 可信度 + 實用性。綠(≥8) / 琥珀(6-7) / 紅(<6)
- `高度相關 / 中度 / 低度`
- `可能改變實務 / 再確認已知 / 仍屬早期 / 結果為陰性`

**四塊核心**
1. **一句話結論** — 在誰身上、比了什麼、發現什麼
2. **明天可以怎麼做** — 具體到病人類型的行動建議
3. **關鍵數據** — 一律給絕對值，能算 ARR／NNT 就附上
4. **要小心** — 臨床陷阱 + 統計紅旗

**與過去文獻的關係**
- `與前作矛盾`（紅）/ `延伸前作`（藍）/ `再確認`（綠）/ `無相關前作`（灰）
- 列出相關前作，點了直接跳過去

**可展開**：PICO 表 + 原文摘要完整中譯

---

## 卡片顏色 = 訊息

顏色回答一個問題：**這篇我該不該花時間？**

| 顏色 | 層級 | 條件 |
|---|---|---|
| 🌹 玫瑰 | **必讀** | 追蹤命中 / 跟已知矛盾 / 高分又高度相關 / 可能改變實務 |
| 🩵 天藍 | 相關 | 一般該讀的 |
| ⬜ 灰 | 可略過 | 低度相關或低分（飽和度 0.8%，視覺上退後）|

**兩個軸不衝突：** 評分 chip 講「品質」，卡片底色講「跟你的相關程度」。
所以會有「低分但必讀」（你的追蹤主題）和「高分但可略過」（跟你無關的領域）。

---

## 🔖 追蹤關鍵字 —— 最重要、也最需要你維護的設定

排序是用「期刊 IF + 研究設計」算的。但**你最在意的東西不一定分數高**。

實例（POUR 論文相關）：

| 論文 | 期刊 | 分數 | 若沒有追蹤機制 |
|---|---|---|---|
| 痔瘡切除術後尿滯留危險因子 | Ann Coloproctol (IF 2.2) | 5.1 | ❌ 被丟掉 |
| 直腸 diclofenac 術後止痛 | Asian J Surg (IF 1.9) | **0.9** | ❌ 被丟掉 |

每日前 10 篇的門檻在 20 分以上。**跟手上論文最相關的文獻，一篇都看不到。**

所以：**命中追蹤清單的論文，無視分數強制納入。**

編輯 `watch.json`（**這是唯一需要人維護的檔案**）：

```json
{
  "limit": 6,
  "keywords": [
    "postoperative urinary retention",
    "hemorrhoidectomy",
    "haemorrhoidectomy",       ← 英美拼法都要放
    "diclofenac"
  ],
  "journals": ["Ann Coloproctol"]    ← 目標期刊，看它們在發什麼
}
```

換研究題目 → 改這個檔案就好。

---

## 網站功能

| 功能 | 說明 |
|---|---|
| **無限捲動** | 一路往下滑會自動載入更早的論文 |
| **▲▼ 翻頁鈕** | 右側浮動。平常滑動 = 一般捲動；按鈕才跳篇 |
| **日期跳轉** | 底部「日期」→ 選某一天，從那天開始往下讀 |
| **🔍 搜尋** | 標題／摘要／關鍵數據／期刊，中英文皆可。空白 = AND |
| **⭐ 稍後精讀** | 星號收藏，底部「收藏」看清單 |
| **● 已讀／未讀** | 停留 >1.2 秒才算讀過。「最新」上顯示未讀數 |

搜尋、收藏、已讀都存在瀏覽器 `localStorage` —— **不需要後端、不花錢**。

---

## 費用

| 項目 | |
|---|---|
| PubMed API | 免費 |
| GitHub Actions / Pages | 免費 |
| Claude API | **約 $15 / 月**（每天約 17 篇 × $0.03）|

**目前 model：`claude-sonnet-5`**
- 8/31 前促銷價 $2 / $10 per MTok
- **⚠️ 9/1 起漲回 $3 / $15** → 屆時約 $22/月

想省錢：把 `fetch_papers.py` 的 `MAX_RESULTS` 從 10 調到 6–8。

**換 OpenAI 不會比較便宜** —— 同層級旗艦（gpt-5.4 / 5.6-terra）每月差不到 $1。
真正的省錢槓桿是「用小模型」，但小模型會在**算 ARR/NNT** 和**抗拒編造**上出問題，不建議。

---

## 檔案結構

```
index.html          網站（單一檔案，含所有 CSS/JS）
manifest.json       PWA
sw.js               Service Worker（離線快取）
icons/              App 圖示

fetch_papers.py     每日收集（GitHub Actions 跑）
backfill.py         回補歷史（手動觸發）
validate_data.py    資料驗證 —— 壞資料不准上網站
tests.py            37 條回歸測試
requirements.txt

journals.json       IF 對照表（journalmetrics 2026，5,817 個 ISSN）— 自動產生，不用動
watch.json          🔖 追蹤關鍵字 — 這個你要自己維護

.github/workflows/
  daily_fetch.yml   每天 UTC 22:00（= 台灣 06:00）自動跑
  backfill.yml      手動觸發，可指定日期區間

data/
  index.json        日期索引
  search.json       搜尋索引（自動產生）
  _cache_v3.json    PMID 快取 — 避免重複付費
  YYYY-MM-DD.json   每日文獻
```

---

## 🛡️ 可靠性機制（2026-07-13 新增）

這一批不是「加功能」，是**防止系統靜默出錯**。

### 1. ARR / NNT 由 Python 計算，模型只負責抽數字

**為什麼：** 模型的多步驟算術會出錯，而 **NNT 和 NNH 搞反是真正的臨床錯誤** —— 你會拿去跟病人講。

```
Claude 只輸出：介入組 25/200，對照組 12/200，事件是壞事

Python 算：
  12.5% vs 6.0%  →  ARR 6.5%  →  NNH 15   ⚠️ 有害
                                   ↑
                    模型很可能寫成「NNT 15」（每 15 人多救 1 個）
                    方向完全相反。Python 不會。
```

**最重要的是它會「拒絕計算」：** 摘要只給 HR / OR / RR 時，ARR 在數學上算不出來。
Prompt 明確禁止換算，Python 直接回報「摘要資料不足」。

卡片上這一區標示「**程式驗算（非模型口述）**」，跟模型口述的 `關鍵數據` 分開。

### 2. 每日成本硬上限

```python
DAILY_HARD_LIMIT = 25   # 每天最多送 25 篇給 Claude
```

追蹤關鍵字設太寬、或某天 Review 暴量 → 會被砍到 25 篇。
**🔖 追蹤命中的優先保留，不會被砍掉。**

### 3. `validate_data.py` —— 壞資料不准上網站

每天 commit 前跑。檢查必要欄位、enum 合法值、score 範圍、陣列型別、
`added_at` 時區格式、`count` 對不對、PMID 有沒有重複、NNT/NNH 是不是正整數。

**驗證失敗 → exit 1 → workflow 不 commit → 網站維持舊資料。**

實測注入 5 個錯誤（score=15、relevance="超級相關"、cautions 給字串、
added_at="剛剛"、count 對不上）→ 5 個全被抓出來，正確擋下。

### 4. `tests.py` —— 46 條回歸測試

**每一條都對應一個真的發生過的 bug**，而且全都是**靜默的**（不報錯，只給你錯答案）：

| 測試 | 防的是 |
|---|---|
| JCO 的 IF > 40 | 正規化撞名把 44.7 變成 2.5 |
| 介入組較差 → NNH 不是 NNT | 方向搞反的臨床錯誤 |
| 只有 HR → 拒絕計算 | 模型硬算 ARR |
| 0 分的 POUR 被 20 篇 Lancet 包圍仍選中 | 追蹤機制失效 |
| 斜體標題不截斷 | `<i>E. coli</i>` 把標題砍半 |
| thinking + text 抓得到 text | Sonnet 5 的回應結構 |
| 維他命 D 論文 → 無相關前作 | TF-IDF 硬掰關聯 |
| 舊資料自動補 `added_at` | 加必要欄位卻沒寫遷移，把 129 篇好資料擋掉 |
| 索引被清空後能完全重建 | 增量維護的索引被覆蓋 → 143 篇論文「消失」|

**每天 workflow 會先跑測試，不過就不 commit。**

### 5. 追蹤命中 → 用研究者角度分析

命中 `watch.json` 的關鍵字時，prompt 會多一段：

```
⚠️ 這篇命中你的追蹤主題（hemorrhoidectomy、urinary retention）

請特別評估：
  1. 是否提供 POUR 的絕對風險（不是只有 OR/RR）
  2. 是否找出可改變的危險因子
  3. 是否比較麻醉、止痛（尤其 diclofenac）、導尿策略
  4. 能不能改變痔瘡術後照護流程
  5. 跟現有文獻的 POUR 發生率相比是高是低
```

追蹤命中不再只是「強制納入」，而是**真的用你的研究問題去讀它**。
編輯 `watch.json` 的 `focus` 欄位可改。

### 6. 醫療免責聲明

日期抽屜底部 + 捲到底時顯示。

### 7. NCBI 呼叫加上重試

GitHub Actions 的 IP 是共用的，NCBI 對共用 IP 會限流（429），偶爾也 5xx。
原本**完全沒有重試** —— 只要 NCBI 打個嗝，`raise_for_status()` 就拋例外，整個 workflow 掛掉。
現在 429 / 5xx / 連線問題都會指數退避重試 4 次。

### 8. `index.json` 改成「衍生資料」

**這是一次真實的教訓，而且是工具鏈造成的。**

打包 zip 時為了清掉測試資料，會 `echo '[]' > data/index.json`。
結果 zip 裡帶著一個**空的索引** —— 一上傳就把使用者真正的索引覆蓋掉。

日檔（`2026-07-05.json` … `2026-07-12.json`）**都還在**，
但 `index.json` 只剩最新那天 → 網站以為只有一天，143 篇論文「消失」。

**根本原因：`index.json` 是「增量維護」的**（讀舊的 → 加一天 → 寫回去）。
只要被覆蓋一次，前面全部消失。

**修法：改成「衍生資料」。** 每次跑都從 `data/` 裡實際存在的日檔完全重建。
現在就算索引被清空，下一次跑就會自己修回來。

`search.json` 本來就是這樣做的。`index.json` 早該一樣。

> **原則：能從別的東西算出來的，就不要另外維護一份狀態。**

⚠️ **zip 裡不再包含 `data/`。** 上傳新版時**不要動那個資料夾**。

### 9. 舊資料自動修補（`heal_legacy`）

**這是一次真實的教訓。**

`added_at` 是後來才加的欄位。我把它設成驗證的必要欄位，卻**沒有寫遷移程式** ——
結果 7/13 之前收集的 129 篇好資料全部被判定不合格，workflow 直接擋下 commit。

現在每次跑都會先自動修補：
- 缺 `added_at` → 用該日的 `generated_at` 補上
- 時間戳沒有時區 → 當成 UTC 轉成台灣時間（`22:03` → 隔天 `06:03+08:00`）
- 冪等，重跑不會有副作用

**教訓：加必要欄位的時候，一定要同時寫遷移。**

---

### 10. 版面：`#feed` 必須明寫 `overflow-x:hidden`（2026-07-18）

**症狀：** 手機上卡片整片往左偏，右側露出一條空白。

**原因：** `#feed` 只寫了 `overflow-y:scroll`。CSS 規範規定，一軸是 `scroll`、
另一軸是預設的 `visible` 時，`visible` 會被算成 `auto` ——
於是只要卡片內有任何元素比視窗寬，`#feed` 就多出一條水平捲軸。
手指滑動稍微帶點橫向，整個 feed 就被推走。

真正撐寬它的是 **PICO 表格**：`.pico` 寫了 `width:100%`，
但預設的 `table-layout:auto` 為了塞下長英文詞（DOI、`ileocolic`、藥名）
會把表格撐超過 100%。

**修法（兩件事都要做）：**

1. `#feed{overflow-x:hidden}` —— 堵死橫向捲動
2. `.pico{table-layout:fixed}` + `overflow-wrap:anywhere` —— 修掉真正溢出的元素

只做第 1 步的話，表格不是縮回來而是**被切掉**，右半邊內容看不到。

另加全域保險絲 `.body{overflow-wrap:anywhere}`。
中文本來就能任意斷行，會撐寬容器的一律是英文長字串。

---

### 11. 收藏改成「自己就是原始資料」（2026-07-18）

**症狀：** 按了星號，收藏清單卻是空的 —— 看起來像「按下去沒有真的收藏」。

**星號本身沒壞。** 它確實有寫進 localStorage，重開也還亮著。
壞的是收藏面板的資料來源：

```js
const idx = await loadIndex();          // ← search.json
const list = idx.filter(x => starred.has(x.p));
```

收藏清單不是從收藏本身畫出來的，而是拿 `search.json` 去篩。
只要索引缺了、舊了、或 404，清單就整片空 ——
而 `loadIndex()` 失敗時是 `catch(() => INDEX = [])`，**靜默**。

A/B 實測（同一份資料，只讓 `search.json` 404）：舊版列出 0 篇，新版 1 篇。

**修法：** 按星號的當下就把標題、期刊、日期、IF、score 一起存進 `cc_star`，
面板直接畫，不碰 `search.json`。

`cc_star` 格式從 `["pmid", ...]` 改成 `{pmid: {t,j,d,f,s,w}}`，
**同時寫了遷移**（舊格式仍讀得進來，標題再去索引補）——
就是第 9 點那個教訓。

已測四個情境：正常、`search.json` 404、舊格式遷移、舊格式 + 404
（最後一種真的補不回標題，但不會崩，會註明「另有 N 篇舊收藏在索引裡找不到」）。

**這跟第 8 點是同一個原則的兩面：**
`index.json` 是衍生資料，所以要能重建；
收藏是**原始資料**，所以不該從別的東西推導出來。
分清楚哪個是哪個，比記住個別修法重要。

---

## ⚠️ 待辦 / 未完成

### 1. 最新版本還沒上傳到 repo
以下功能是最近做的，**repo 上還是舊版**，要上傳才會生效：
- 🔖 追蹤關鍵字
- 🔍 搜尋 / ⭐ 收藏 / ● 已讀
- 🔗 與過去文獻的關係（TF-IDF 比對）
- 三層語意色（玫瑰／天藍／灰）
- 時區修正、`added_at`

上傳後**要跑一次 workflow**，追蹤標記和 `search.json` 才會產生。

### 2. ~~回補歷史文獻從未成功跑完~~ ✅ 已完成
**7/05 – 7/13 共 143 篇已在資料庫裡。** 今天（7/13）用新版跑的 14 篇通過完整驗證。

**正確跑法：** Actions → 回補歷史文獻 → Run workflow

| 欄位 | 填 |
|---|---|
| 起始日 | `2026-06-01` |
| 結束日 | 留空 |
| **每天分析幾篇** | **`10`**（不是 1000！這個數字直接乘上費用）|
| **試算模式** | **先勾 ✅**（不花錢，只估價）|

試算看數字 OK 再取消勾選跑第二次。約 $13。

### 3. journals.json 的名稱比對偶有誤差
ISSN 比對可靠（5,817 筆）。名稱比對是後備，因原始資料雙欄排版錯亂偶有錯值。
發現 IF 明顯不對 → 直接改 `journals.json` 的 `by_issn`。

---

## 開發過程中修掉的坑（給接手的人避雷）

| 問題 | 症狀 | 真正原因 |
|---|---|---|
| **Model 名稱** | Actions 一直 404 | `claude-sonnet-4-20250514` 已廢棄。**不是防火牆** |
| **推理模型的回應結構** | `KeyError: 'text'` | Sonnet 5 會先回 `thinking` 區塊，不能假設 `content[0]` 是文字 |
| **重試燒錢** | 每篇付 3 次錢卻拿不到東西 | 程式碼 bug 屬「必然失敗」，重試無用。現在只重試網路/JSON 錯誤 |
| **時區** | 日期標成前一天、時間差 8 小時 | GitHub Actions 跑在 UTC。全部改用台灣時區（UTC+8）|
| **IF 全部查不到** | log 一堆 `IF ?` | 用期刊縮寫比對配不上。改用 **ISSN** |
| **IF 值錯得離譜** | JCO 顯示 2.5（實為 44.7）| 名稱正規化砍掉 `journal`/`of`/`the`，導致「Journal of Clinical Oncology」和「Clinical Oncology」撞成同一個 key。**絕對不可以砍這些字** |
| **手機滑動亂跳** | 一滑跳好幾篇 | 兩層捲動容器 + `scroll-snap: mandatory` 打架。拆掉巢狀捲動 |
| **log 看起來卡住** | 跑五分鐘沒輸出 | Python 對管線做區塊緩衝。加 `python -u` |
| **相關文獻硬掰關聯** | 無關的論文被配在一起 | 相似度門檻 0.10 太鬆（兩篇只因都提到 colorectal cancer）。拉到 **0.15** |
| **選片只看分數** | 每天只拿到「最新的」10 篇 | 「最新」≠「重要」。改成掃描全部 → 排序 → 取最值得讀的 |

---

## 設計原則（不要違反）

1. **不准編造。** PubMed 摘要不會寫盲法、ITT、失訪率、利益衝突。
   Prompt 明確禁止模型猜測 → 沒寫的列進「摘要沒寫」，提醒需查全文。
   **醫師會拿這個做臨床判斷，寧可誠實說不知道。**

2. **顏色必須承載訊息。** 裝飾性的顏色輪換是噪音。
   評分紅綠燈 = 品質；卡片底色 = 跟你的相關程度。兩軸不混用。

3. **相關性不由 IF 決定。** 追蹤命中的論文無視分數強制納入。

4. **絕對值優先。** 關鍵數據一律給 ARR / NNT，不只給相對風險和 p 值。
